[project]
name = "pipeline"
version = "0.2.0"
description = "Automated spike sorting pipeline"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "Tuguldur" }
]

dependencies = [
    "numpy>=2.3",
    "pyyaml>=6.0",
    "loguru>=0.7.0",
    "spikeinterface[full]>=0.101.0",
    "probeinterface>=0.2.21",
    "uv>=0.9.0",
    "jupyter>=1.1.1",
    "ipywidgets>=8.1.8",
    "open-ephys-python-tools>=1.0.1",
    "cmap>=0.7.2",
]

[project.optional-dependencies]
full = [
    "kilosort>=4.1.7",
    "torch>=2.0",
]

[project.scripts]
pipeline-run = "pipeline.run_script:main"
pipeline-gen_xml = "pipeline.xml_script:main"
gen_xml = "pipeline.gen_xml:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu128" }
